# System Tests

This directory will contain end-to-end system tests that verify the complete application workflow.

## Future Test Categories

- Complete CLI workflow tests
- GUI workflow tests
- File I/O integration tests
- Multi-format export tests
